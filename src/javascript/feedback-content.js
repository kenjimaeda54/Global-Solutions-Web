

const submitData = () =>{
     const imgUserSelected = document.querySelector('input').files[0];
     const hotelSelected = document.querySelector('.hotel-selected').value;
     const selectedEvaluation = document.querySelector(".selected-evaluation").value;
     const imgDiv = document.querySelector(".img-user");
     const formated = URL.createObjectURL(imgUserSelected);
 

} 